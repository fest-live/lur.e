[**@fest-lib/lure v0.1.35**](../README.md)

***

[@fest-lib/lure](../README.md) / maybeStartThemeEngine

# Function: maybeStartThemeEngine()

```ts
function maybeStartThemeEngine(): void;
```

Defined in: lur.e/src/design/color/ThemeEngine.ts:17

Opt-in autostart only.
This module is re-exported from `fest/lure` root, so unconditional side effects
here can start a competing theme-color loop in host apps.

## Returns

`void`
